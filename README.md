# Codex PTY Wrapper (Interactive)

Purpose: run Codex interactive sessions under a wrapper that can detect “phase done” and exit automatically, so higher-level orchestration scripts do not block waiting for manual `Ctrl+C`.

See:
- `ai/wrapper/implementation_plan.md` for the design and milestone plan.
- `ai/wrapper/decisions.md` for open decisions to finalize before implementation.

